VITE_API_URL=http://localhost:8000

# Optional. If set, the app skips the login page and uses this key for
# every request — convenient for local dev. Leave blank to sign in via
# the login page instead (the key is then kept in sessionStorage, not
# baked into the bundle).
VITE_ARGUS_API_KEY=
