services:
  backend:
    image: argus-backend:local
    container_name: argus-backend
    network_mode: "host"
    volumes:
      - argus-data:/app/data
      - ./.env:/app/.env:ro
    restart: unless-stopped

volumes:
  argus-data:
